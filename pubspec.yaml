name: push_ohos
description: "OpenHarmony / HarmonyOS NEXT push notification module"
version: 1.0.0

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  push_core:
    path: ../push_core
  logger: ^2.5.0
  synchronized: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
